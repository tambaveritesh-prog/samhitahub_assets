"""
SamhitaHub Studio Backend
==========================

Root application package for the SamhitaHub Studio AI automation backend.

This package is intentionally kept thin. It exists so that `app` is
importable as a namespace package and so that package-level metadata
(version, title) can be defined in a single place and reused by
`app.main`, `pyproject.toml` tooling, and documentation generation.
"""

__version__ = "0.1.0"
__title__ = "SamhitaHub Studio Backend"
